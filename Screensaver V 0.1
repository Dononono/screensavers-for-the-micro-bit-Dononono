let screenSaver = 0
let countDown = game.startCountdown
// The string of text that appears when the screen is awake

basic.forever(function () {
    basic.showString("OOGA OOGA OOGA CHAKA")
})


// Counting the amount of time that the A button is held down 
// And selecting the values for screenSaver

function buttonIsPressed(Button, A) {
    game.startCountdown(2000)
    if (countDown = 0 && buttonIsPressed(Button.A)) {
        Math.randomRange(0, 5)


    })
